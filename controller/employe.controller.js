const db = require("../models");
const emplye = db.emplye;
const Op = db.Sequelize.Op;

// Create and Save a new emplye
exports.create = (req, res) => {
  
};

// Retrieve all emplye from the database.
exports.findAll = (req, res) => {
  
};

// Find a single emplye with an id
exports.findOne = (req, res) => {
  
};

// Update a emplye by the id in the request
exports.update = (req, res) => {
  
};

// Delete a emplye with the specified id in the request
exports.delete = (req, res) => {
  
};